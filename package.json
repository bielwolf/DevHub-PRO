{
  "name": "DevHub-Pro",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:reset": "npx sequelize-cli db:migrate:undo:all --config src/db/config/config.json && npx sequelize-cli db:migrate --config src/db/config/config.json && npx sequelize-cli db:seed:all --config src/db/config/config.json"

  },
  "dependencies": {
    "express": "^5.2.1",
    "knex": "^3.1.0",
    "path": "^0.12.7",
    "sequelize": "^6.37.7",
    "sqlite3": "^5.1.7",
    "url": "^0.11.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "sequelize-cli": "^6.6.3"
  }
}
